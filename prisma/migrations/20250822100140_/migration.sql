BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Reading] ADD [jpno] NVARCHAR(1000),
[note] NVARCHAR(1000),
[status] NVARCHAR(1000) NOT NULL CONSTRAINT [Reading_status_df] DEFAULT 'unread';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
